# CHANGELOG

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。0.x 为动态插件原型期（会话级、重启即失），1.0 起为可安装的组合包（bundle）。

## 1.0.2 — 2026-08-21

**查看器引入 marked 渲染完整 Markdown**：修复代码块（```` ``` ````）不渲染、行内代码（`` ` ``）原样显示的问题。

- 随包分发 `lib/vendor/marked.min.js`（marked v12.0.2，MIT），`/workorders-vendor/marked.min.js` 静态路由提供，页面浏览器端渲染
- 解析新增 fenced 代码块（```` ``` ```` / `~~~`，含语言标注，深色代码块样式）
- 段落/标题/列表/引用/勾选清单内容改用 `marked.parseInline`：支持 `**粗体**`、`` `行内代码` ``、`[链接](url)` 等；表格仍走专用渲染（场景标签/不换行列）
- marked 加载失败时自动回退原转义渲染，不影响查看器可用性

## 1.0.1 — 2026-08-21

**行为变更**：点「✓ 通过 / ✓ 验收通过」时，插件直接勾选工作单对应项并关闭面板 —— 待办由文件状态推导，勾选后**立即消失**，无需等待 AI 响应。

- Host：`/workorders-api` 增加 POST（勾选「工作单整体评审通过」/「最终验收」）；写入显式携带 workspace-write 策略
- 客户端：通过流程 = ① POST 勾选 → ② `conversation.send` 发确认文案 → ③ 关闭面板
- 待验收待办条件改为 `勾选数 === 6`（验收通过勾「最终验收」后进入 7 → 待办消失）
- 注：这是插件唯一一次写工作单文件，属用户明确决定

## 1.0.0 — 2026-08-21

**静态化发布**：从动态插件（wrkflw-2/pkg-28）移植为官方 bundle 格式，可经 `dsh plugin add` 本地安装。

- Host 半：`/workorders-viewer`（HTML 查看器，动态生成）+ `/workorders-api`（状态 JSON）+ `tools/result` 监听立即重扫
- 客户端半：`__ModuleLoader__` Web 模块；数据走 fetch，确认消息走 `conversation.send`（静态模块无 guard 限制）
- 功能与动态版一致：悬浮胶囊、待办（待评审/待验收）、工作单状态列表（勾选数映射）、HTML 查看器、打回预填输入框
- 移除：动态专用 `harness.handle`/`host.call` RPC、`workorders/.workflow-state.json` 记账（v3 起已为纯文件驱动）

## 0.6.x — 动态版：HTML 查看器（pkg-24 ~ pkg-28）

- 0.6.0 查看工作单改为打开 HTML 查看器（webServer 路由动态生成，移植 `template/build-workorders.cjs`）
- 0.6.1 修复：返回列表（INITIAL_FILE 用后清空）、进度 undefined、场景标签不换行
- 0.6.2 修复：`renderBlocks` 引用未定义变量
- 0.6.3 返回列表清除 URL `?file=` 参数；任务切片「依赖/状态」、测试用例「所属切片」列不换行
- 0.6.4 任务切片「序号」、测试用例「所属切片」表头不换行；列表点击卡片时 URL 写入 `?file=`

## 0.5.x — 动态版：工作单为本重构（pkg-20 ~ pkg-23）

- 0.5.0 移除插件自造的记账与「待确认」概念；状态 = 进度清单勾选数映射；待办 = 评审门 / 验收门；轮询 1 分钟 + AI 行为立即重扫 + 「🔄 更新」按钮
- 0.5.1 状态标签改名：设计中→确认中、已评审（等实现）→实现中、实现完成（等验收）→待验收

## 0.4.x — 动态版：UI 打磨（pkg-11 ~ pkg-19）

- 0.4.0 详情面板改为悬浮窗口（可拖拽，后因体验问题放弃）
- 0.4.1 拖拽改 Pointer Events + 指针捕获
- 0.4.2 面板固定垂直居中、右侧对齐
- 0.4.3 面板精简为「有 N 项待办 / 暂无待办」（后被回退，恢复完整面板 + 胶囊精简）
- 0.4.4 移除「知道了」；待办完全状态驱动
- 0.4.5 打回后待评审待办持续存在
- 0.4.6 打回/驳回改为预填输入框（句号改冒号），确认/通过保持一键发送
- 0.4.7 打回后自动关闭面板
- 0.4.8 工作单列表只显示文件名+状态；打回模板冒号

## 0.3.x — 动态版：健壮性（pkg-8 ~ pkg-10）

- 0.3.0 切换会话不再报错闪现（refresh token + 切换重置）；无 workorders 目录的工作区自动隐藏插件
- 0.3.1 修复记账写入被沙箱拒绝（显式 workspace-write 策略）
- 0.3.2 通过/打回时同步记录 confirmed，避免评审后冒出假待办

## 0.2.x — 动态版：提醒重设计（pkg-5 ~ pkg-7）

- 0.2.0 按「提醒」定位重做：悬浮胶囊 + 右侧面板 + 待办事项 + 确认/评审通过对话框发送文案（agent.send）
- 0.2.1 修复 useSessions 从 props 取值
- 0.2.2 聊天发送移到 Host 侧（绕开客户端 Context 拦截）

## 0.1.x — 动态版：雏形（pkg-1 ~ pkg-4）

- 0.1.0 工作单自动弹出 + 确认闭环 + 评审门 + 状态条（v1 设计，后被用户重定向为提醒版）
- 0.1.1 修复客户端返回形态、括号、Checklist props
- 0.1.2 修复评审弹窗时序与评审后误报待确认
